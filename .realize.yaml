settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: independent-core-layer-golang
  path: .
  commands:
    run:
      status: true
  watcher:
    extensions:
    - go
    paths:
    - /
    ignored_paths:
    - .git
    - .realize
    - vendor
